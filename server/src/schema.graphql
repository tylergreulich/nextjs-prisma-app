# import * from './generated/prisma.graphql'

type Mutation {
  createItem(
    title: String!
    description: String!
    price: Int!
    image: String
    largeImage: String
  ): Item!
  register(email: String!, password: String!): User
}

type Query {
  items: [Item]!
}
